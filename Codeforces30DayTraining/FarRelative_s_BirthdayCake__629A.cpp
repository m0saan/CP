#include <iostream>
#include <map>
#include <cmath>
using namespace std;
int main(){
    int n;
    cin >> n;
    string S;
    map<int,int> m;
    for (int i = 0; i < n; ++i) {
        cin >> S;
        for (int j = 0; j < S.size(); ++j) {
        }
    }
    return 0;
}
