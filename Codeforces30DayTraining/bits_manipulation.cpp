#include <iostream>
using namespace std:
int main(){
	int n, in;
	cin >> n;
	vector<int> v(n);
	for (size_t i=0; i<n; ++i){
		cin >> v[i];
		bool ans = false;
		for (size_t j=1; j <= ; ++j)
			if (i == v[v[v[i]]]) ans = true;
	}
	if (ans) cout << "YES" << endl;
	else cout << "NO" << endl;
	return 0;
}

