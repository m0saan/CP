#include <iostream>
#include <string>
#include <vector>

int main() {

    // Fast input-output
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    
    return 0;
}